# Handoff Summary — 2026-04-15 후반

> **다음 세션 지시사항:**
> - 이전 세션에서 이어받는 작업입니다.
> - 아래 요약을 읽고 현재 상황을 파악하세요.
> - 사용자에게 간단히 현황 브리핑 후 "이어서 진행할까요?" 물어본 다음 승인되면 작업 재개.
> - 자세한 스냅샷은 `C:\AUTO\CREW\STATE.md` 가 정본입니다.

## Project / Topic
**CrewLife** — 승무원 전용 AI 스케줄 관리·공유 앱. Flutter(iOS/Android) + Supabase + Claude Sonnet 4.6 Vision OCR + 카카오 로그인 + AdMob. 작업 디렉토리 `C:\AUTO\CREW\`, Flutter 앱 루트 `C:\AUTO\CREW\app\`.

## Goal
Phase 2-A (AI OCR)의 end-to-end 플로우를 실기기에서 완성: 갤러리 사진 업로드 → Claude Vision 파싱 → 캘린더에 자동 표시. 카카오 로그인/DB 저장은 일부러 뒤로 미룸 (현재 `LocalFlightStore` 메모리 전용).

## Completed Work

### Edge Function 강건화 (`supabase/functions/parse-schedule/index.ts`)
- `max_tokens` 4096 → **8192** (월간 스케줄 전체 출력 보장)
- `extractFlights()` 추가 — 엄격 `JSON.parse` 실패 시 `"flights"` 배열을 brace-depth 로 탐색하여 개별 flight 객체 단위로 복구. 일부가 망가져도 나머지 수집.
- 시스템 프롬프트에 LO/ATDO/STBY 매핑 규칙 명시 (LayOver 행도 `duty_type="LAYOVER"` + null 필드로 수집)
- `--no-verify-jwt` 로 배포 완료 (개발 단계 로그인 우회)

### 로컬 저장소 + UI
- `lib/features/ocr/local_flight_store.dart` — `ChangeNotifier` 기반 메모리 store (카카오 로그인 붙으면 Supabase insert 로 교체)
- `ocr_review_view.dart` **삭제** — 파싱 결과는 바로 store 에 들어가고 캘린더 화면으로 pop
- 진행 문구 `"Claude가 스케줄을 분석 중..."` → `"사진 분석 및 업로드 중..."`
- `parse_schedule_client.dart` — `http_parser.MediaType` 로 multipart content-type 명시 (이전 415 오류 해결)

### 캘린더 셀 디자인 (example/1.jpg 과 동일한 층층 스택)
- `schedule_tab.dart` 전면 재작성 — `TableCalendar.calendarBuilders` 로 `_DayCell` 커스텀
- 셀 구조 = 우측 상단 날짜 숫자 → 항공사 브랜드 컬러 header bar(편명) → route(`DPS-ICN`) → time(`01:05-09:00`)
- `LayoutBuilder` 로 `rowHeight` 86~150 사이 자동 계산 (6행 균등 분배)
- `toolbarHeight` 76 → 48, 인삿말 2줄 → 1줄, `headerPadding` 축소

### 마지막 턴 (4번째 user request) 변경
1. **3건 이상 시간 숨김** — `_DayCell` 에서 `showTime = flights.length <= 2`, `_FlightStripe` 에 전달. KE1803/1806/1811 overflow 해결.
2. **LO/ATDO 연속 구간 시각 병합** — `_runPositionFor(day, duty)` 로 single/start/middle/end 판별. 라벨은 single/start 만, middle/end 는 빈 bar. border radius 를 start=왼쪽만 / middle=각지게 / end=오른쪽만 으로 적용.
3. **March 2026 헤더** — `titleCentered: true`, fontSize 16 → 19 (약 +19%).
4. **인삿말** — `"$nickname님"` → `"$nickname님, 좋은하루되세요"`.

### 항공사 감지 엄격화 (`airline_registry.dart`)
- 정규식을 `^([A-Z0-9][A-Z]|[A-Z][A-Z0-9])\d{2,4}[A-Z]?$` 로 변경.
- `KE0115` ✓, `KE` / `KE1` / `KELAX` ✗ → 브랜드 컬러 false positive 방지.

## In-Progress Work
**실기기 검증 대기 중**. 사용자가 자리를 비우면서 중단. 위 변경들이 실제로 1.jpg / 3.jpg / 4.jpg 업로드 시 눈으로 확인되는지 아직 미검증.

## Remaining Work (검증 후 예상)
- 실기기에서 4가지 변경 확인
- 시각 gap 이 거슬리면 `cellMargin` 추가 조정
- 카카오 로그인 연동 → `LocalFlightStore` 를 Supabase `schedules` insert 로 교체
- `saveConfirmedFlights` 실제 구현 복원 (`parse_schedule_client.dart:155` TODO)
- `ai_parsed_at` + `source_hash` 1회 가드 재활성화
- Phase 2-B: QuickAddSheet 실제 저장 연결

## Important Context / 제약
1. **AI OCR은 최초 1회만** (현재 가드 비활성, 추후 DB 플래그로 복원)
2. **Anthropic API 키는 Supabase Edge Function secrets 에만** — 앱 번들 절대 금지
3. **모델**: `claude-sonnet-4-6` (Vision 지원)
4. **Supabase 키 우선순위**: `SUPABASE_PUBLISHABLE_KEY` > `SUPABASE_ANON_KEY` (하위 호환)
5. **개인 프로젝트 톤앤매너** — 직장 내 겸업 리스크 최소화
6. **Flutter PATH 수동 설정**: `export PATH="/c/src/flutter/bin:$PATH"` 매 세션
7. **현재 `flutter analyze` 0 issues**

## Key Documents / Files
- `C:\AUTO\CREW\STATE.md` — **작업 재개 스냅샷 (정본)**
- `C:\AUTO\CREW\app\lib\features\schedule\schedule_tab.dart` — 캘린더 커스텀 렌더 (최근 집중 수정 파일)
- `C:\AUTO\CREW\app\lib\features\ocr\ocr_upload_view.dart` — 업로드 화면
- `C:\AUTO\CREW\app\lib\features\ocr\parse_schedule_client.dart` — Edge Function 래퍼
- `C:\AUTO\CREW\app\lib\features\ocr\local_flight_store.dart` — 메모리 store
- `C:\AUTO\CREW\app\lib\core\airlines\airline_registry.dart` — 엄격화된 정규식
- `C:\AUTO\CREW\supabase\functions\parse-schedule\index.ts` — 강건화된 JSON 복구 로직
- `C:\AUTO\CREW\example\1.jpg` / `3.jpg` / `4.jpg` — 디자인 레퍼런스 + 회귀 테스트 이미지

## Next Steps
1. `STATE.md` 한번 읽고 현재 상황 파악
2. 사용자에게 "실기기에서 1.jpg/3.jpg/4.jpg 다시 업로드해서 4가지 변경(시간 숨김, LO 병합, 헤더 센터+크게, 인삿말) 확인하시겠어요?" 물어보기
3. 스크린샷 받으면 `example/` 폴더에 저장 후 이슈별로 수정 이어가기
